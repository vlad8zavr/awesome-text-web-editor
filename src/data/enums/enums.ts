export enum KEYS {
    tilda = '`',
    bold = 'b',
    italic = 'i',
    tab = 'Tab',
};

export enum TEXTMODE {
    paragraph = 'paragraph',
    code = 'code',
};

export enum FORMAT {
    bold='bold',
    italic='italic',
    underline='underline',
    code='code',
}